version: '3'

volumes:
  celulas_responsaveis_local_postgres_data: {}
  celulas_responsaveis_local_postgres_data_backups: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: celulas_responsaveis_local_django
    container_name: celulas_responsaveis_local_django
    platform: linux/x86_64
    depends_on:
      - postgres
      - redis
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - "8000:8000"
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: celulas_responsaveis_production_postgres
    container_name: celulas_responsaveis_local_postgres
    volumes:
      - celulas_responsaveis_local_postgres_data:/var/lib/postgresql/data:Z
      - celulas_responsaveis_local_postgres_data_backups:/backups:z
    env_file:
      - ./.envs/.local/.postgres

  docs:
    image: celulas_responsaveis_local_docs
    container_name: celulas_responsaveis_local_docs
    platform: linux/x86_64
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./celulas_responsaveis:/app/celulas_responsaveis:z
    ports:
      - "9000:9000"
    command: /start-docs

  redis:
    image: redis:6
    container_name: celulas_responsaveis_local_redis

  celeryworker:
    <<: *django
    image: celulas_responsaveis_local_celeryworker
    container_name: celulas_responsaveis_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: celulas_responsaveis_local_celerybeat
    container_name: celulas_responsaveis_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: celulas_responsaveis_local_flower
    container_name: celulas_responsaveis_local_flower
    ports:
      - "5555:5555"
    command: /start-flower
